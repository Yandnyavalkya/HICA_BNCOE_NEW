# MongoDB (same as Node backend)
MONGODB_URI=mongodb://localhost:27017/hica
MONGODB_DB_NAME=hica

# JWT
JWT_SECRET=CHANGE_ME_SUPER_SECRET
JWT_ALGO=HS256
JWT_EXPIRE_MINUTES=1440

# Admin (created on first run)
ADMIN_EMAIL=admin@hica.com
ADMIN_PASSWORD=admin123

# CORS
FRONTEND_ORIGINS=http://localhost:5173

# Optional: Cloudinary for gallery uploads
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=

# Optional: SMTP for event notification emails to subscribed users
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_USE_TLS=true
# EMAIL_FROM=noreply@hica.com

# Server
PORT=8000
