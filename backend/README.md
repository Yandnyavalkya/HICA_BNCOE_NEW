# Backend (deprecated)

The Node.js backend has been replaced by **backend_fastapi** (Python/FastAPI).

- API and deployment: see **backend_fastapi/** and root **README.md**.
- Render deployment: backend service uses `backend_fastapi/`.
